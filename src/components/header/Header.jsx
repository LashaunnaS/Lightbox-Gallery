import React from 'react';
import { HeaderWrapper, HeadTitle } from './HeaderStyles';

const Header = () => {
    return (
        <HeaderWrapper>
            <HeadTitle>The Gallery</HeadTitle>
        </HeaderWrapper>
    )
  }

  export default Header;